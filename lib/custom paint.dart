import 'package:flutter/material.dart';

class Sunpaint extends StatelessWidget {
  const Sunpaint({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        height: 400,
        width: 400,
        color: Colors.blue,


    )
    );
  }
}

